# RAG Core
# ChromaDB: Using 0.4.15 for maximum stability (telemetry issues fixed in older version)
# Alternative: chromadb==0.4.18 (newer but may have telemetry issues)
chromadb==0.4.15
sentence-transformers==2.5.1

# PDF / Text Processing
pdfplumber  # Primary PDF parser (best quality)
pypdfium2   # Fallback PDF handler
PyPDF2      # Lightweight PDF text extraction

# LLM / Configuration
google-generativeai
python-dotenv

# UI
streamlit

# Structured Output / Agent Communication
dataclasses-json
orjson
